<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Android 12 (API 31) 起的新蓝牙运行时权限。
         neverForLocation：声明扫描结果不用于推算位置，可免去定位权限（企业开发标准做法，
         代价是扫不到 iBeacon 等定位类广播，第 2 课详讲）。 -->
    <uses-permission android:name="android.permission.BLUETOOTH_SCAN"
        android:usesPermissionFlags="neverForLocation" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />

    <!-- Android 11 (API 30) 及以下的旧权限体系：蓝牙本身是安装时权限，
         但扫描必须持有运行时定位权限（历史原因：BLE 广播可用于室内定位）。 -->
    <uses-permission android:name="android.permission.BLUETOOTH"
        android:maxSdkVersion="30" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN"
        android:maxSdkVersion="30" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"
        android:maxSdkVersion="30" />

    <!-- 第 9 课：后台保活。Android 没有 iOS 那种系统代管的后台蓝牙，
         息屏/切后台后系统会限制甚至挂起 App 的蓝牙活动。要长时间保持连接
         必须起「前台服务」（带常驻通知）。以下是接入所需权限：
         - FOREGROUND_SERVICE：Android 9+ 起前台服务必备；
         - FOREGROUND_SERVICE_CONNECTED_DEVICE：Android 14+ 要求声明前台服务类型。
         真正的 Service 类需通过平台通道或插件（如 flutter_foreground_task）实现，
         本教学工程只声明配置、在讲义讲清原理，不落地 Service 代码。 -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_CONNECTED_DEVICE" />
    <!-- Android 13+ 通知运行时权限：前台服务的常驻通知需要它 -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:label="wb_ble_app"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
